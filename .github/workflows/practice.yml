name: "Test PR Comment"
on:
  pull_request:
jobs:
  testing_action:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
      with: 
        repo-token: ${{ secrets.GITHUB_TOKEN }}
    - uses: ./

